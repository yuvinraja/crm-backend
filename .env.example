# Example environment variables (DO NOT PUT REAL SECRETS HERE)
# Copy to .env and fill in real values locally. In production (Render), set via Dashboard.

# Server
PORT=3000
NODE_ENV=development

# Database
MONGO_URI=mongodb+srv://<user>:<password>@crm-cluster1.njhvbwm.mongodb.net/crm?retryWrites=true&w=majority&appName=crm-cluster1

# OAuth (Google)
GOOGLE_CLIENT_ID=<google-client-id>
GOOGLE_CLIENT_SECRET=<google-client-secret>
# For local dev
GOOGLE_CALLBACK_URL=http://localhost:3000/auth/google/callback
# For production update in Google Console (example):
# GOOGLE_CALLBACK_URL=https://your-service.onrender.com/auth/google/callback

# Session
SESSION_SECRET=<generate-a-long-random-string>

# CORS: Comma separated list. Use explicit domains in production.
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

